import { Column, Entity, PrimaryGeneratedColumn } from 'typeorm';

@Entity()
export class {{titleCase-removePlural name}} {
  @PrimaryGeneratedColumn()
  id: number;

  @Column({ default: true })
  isActive: boolean;
}
